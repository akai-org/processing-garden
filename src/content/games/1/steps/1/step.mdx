import { ResultInput } from '../../../../../components';
import { correctValue, codeTemplate } from './step.template';
import Editor from '@monaco-editor/react';

# 123

```js
import * as p5 from 'p5';

  function setup() {
    createCanvas(400, 400);
  }

  function draw() {
```

<div
  style={{
    background: 'rgb(26, 32, 43)',
    marginTop: '-20px',
    marginBottom: '-20px',
  }}
>
  <Editor
    options={{
      minimap: {
        enabled: false,
      },
      fontSize: 16,
      wordWrap: 'on',
    }}
    width="100%"
    height="400px"
    language="javascript"
    theme="vs-dark"
    onChange={props.onChange}
  />
</div>

```js
  }

  window.setup = setup;
  window.draw = draw;
;
```
