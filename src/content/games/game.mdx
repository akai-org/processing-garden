import { ResultInput } from '../../components';
import { correctValue, codeTemplate } from './game.template';
import Editor from '@monaco-editor/react';

```js
import * as p5 from 'p5';

  function setup() {
    createCanvas(400, 400);
  }

  function draw() {
```

<div
  style={{
    background: 'rgb(39, 40, 34)',
    marginTop: '-20px',
    marginBottom: '-20px',
  }}
>
  <Editor
    style={{
      background: 'rgb(39, 40, 34)',
      paddingLeft: '32px',
      marginTop: '-20px',
      marginBottom: '-20px',
    }}
    width="100%"
    height="400px"
    language="javascript"
    theme="vs-dark"
  />
</div>

```js
  }

  window.setup = setup;
  window.draw = draw;
;
```
