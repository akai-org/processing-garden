import { SandpackPreview, SandpackProvider } from '@codesandbox/sandpack-react';
import { codeTemplate, correctValue } from './meta';
import { codeTemplate as renderTemplate } from './template1';
import { ResultInput, SandpackWrapper } from '../../../components';
import { Box } from '@chakra-ui/react';

<Box mt={5} mb={5}>
  Twój pierwszy szkic w p5js.
</Box>

```js
import * as p5 from 'p5';

function setup() {
  createCanvas(100, 100);
}

function draw() {
  background(0, 255, 0);
}

window.setup = setup;
window.draw = draw;
```

<SandpackWrapper>
  <SandpackProvider
    customSetup={{
      entry: '/index.js',
      dependencies: { p5: 'latest' },
      files: {
        '/index.js': {
          code: renderTemplate(),
          active: true,
        },
      },
    }}
  >
    <SandpackPreview />
  </SandpackProvider>
</SandpackWrapper>

<Box mt={20} mb={5}>
  Przećwicz to sam - ustaw wartość background równą 200:
</Box>

```js
import * as p5 from 'p5';

function setup() {
  createCanvas(100, 100);
}

function draw() {
```

<ResultInput
  onChange={props.handleChange}
  correctValue={correctValue}
  onSuccess={props.onSuccess}
  template={codeTemplate}
/>

```js
}

window.setup = setup;
window.draw = draw;
```
