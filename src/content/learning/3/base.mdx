import { SandpackPreview, SandpackProvider } from '@codesandbox/sandpack-react';
import { codeTemplate, correctValue } from './meta';
import { codeTemplate as renderTemplate } from './template1';
import { ResultInput, SandpackWrapper } from '../../../components';
import { Box } from '@chakra-ui/react';

<Box mt={5} mb={5}>
  Obsługa eventów (klik myszką, wciśnięcie klawisza). Kliknij myszką w
  wyrenderowany obraz i sprawdź jak zmienia się kolor!
</Box>

```js
import * as p5 from 'p5';

function setup() {
  createCanvas(100, 100);
}

function draw() {
  background(0, 255, 0);
  ellipse(50, 50, 80, 80);
  if (mouseIsPressed) {
    fill(0);
  } else {
    fill(255);
  }
}

window.setup = setup;
window.draw = draw;
```

<SandpackWrapper>
  <SandpackProvider
    customSetup={{
      entry: '/index.js',
      dependencies: { p5: 'latest' },
      files: {
        '/index.js': {
          code: renderTemplate(),
          active: true,
        },
      },
    }}
  >
    <SandpackPreview />
  </SandpackProvider>
</SandpackWrapper>

<Box mt={20} mb={5}>
  Obsługa eventów klawiszowych - większość naszych gier będzie się opierać na
  klawiatrze. Wywołajmy akcje na wciśnięcie klawiszy. Dokończ podany kod dodając
  akcje dla górnego klawisza.
</Box>

```js
import * as p5 from 'p5';

function setup() {
  createCanvas(100, 100);
}

function draw() {
  background(0, 255, 0);
  ellipse(50, 50, 80, 80);
}

function keyPressed() {
  switch (keyCode) {
    case LEFT_ARROW:
      fill(0);
      break;
    case RIGHT_ARROW:
      fill(100);
      break;
```

<ResultInput
  onChange={props.handleChange}
  correctValue={correctValue}
  onSuccess={props.onSuccess}
  template={codeTemplate}
/>

```js
      fill(180);
      break;
    case DOWN_ARROW:
      fill(250);
      break;
  }
}

window.setup = setup;
window.draw = draw;
window.keyPressed = keyPressed;
```
