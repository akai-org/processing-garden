export const challengeTitle = 'asdf';

export const codeTemplate = () => `
import * as p5 from 'p5';

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(0, 255, 0);
}

window.setup = setup
window.draw = draw
`;
